[phases.setup]
nixPkgs = ["ffmpeg", "nodejs_20"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 4 -b 0.0.0.0:$PORT app:app --timeout 300 --worker-class gthread --threads 4"
